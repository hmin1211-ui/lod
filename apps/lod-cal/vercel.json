{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "echo \"No install required\"",
  "buildCommand": "mkdir -p vercel-static/calculator && cp index.html styles.css app.js vercel-static/ && cp index.html styles.css app.js vercel-static/calculator/",
  "outputDirectory": "vercel-static",
  "rewrites": [
    {
      "source": "/calculator",
      "destination": "/calculator/index.html"
    },
    {
      "source": "/calculator/(.*)",
      "destination": "/calculator/$1"
    }
  ]
}
